package previmedical.it.leetcode.problems.other;

public class SlidingWindowCountSubsequentSum {

    /*
        Given an array of integers of size ‘n’.
        Our aim is to calculate the maximum sum of ‘k’
        consecutive elements in the array.

        Input  : arr[] = {100, 200, 300, 400} k = 2
        Output : 700
     */


    /*
        what if array is null or empty?
            return 0
        what if k is 0 or negative?
            return 0
        does the array fit in memory?
            YES: OK
            NO: Divide it in chunks and analyze each chunk.

        what if k is larger than array.length?
            return the sum of the array
     */

    public int maxSubsequentSum(int[] array, int k) {

    }

}
